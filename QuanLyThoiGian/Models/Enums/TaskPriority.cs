namespace QuanLyThoiGian.Models.Enums
{
    public enum TaskPriority
    {
        Low = 1,      // Thấp (Xanh lá)
        Medium = 2,   // Trung bình (Vàng)
        High = 3,     // Cao (Cam)
        Urgent = 4    // Khẩn cấp (Đỏ)
    }
}
